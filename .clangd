CompileFlags:
  # Use C++23 standard
  Add: [-std=c++23]
  
  # Alternatively, you can use the GCC-style flag:
  # Add: [-std=c++2b]
  
  # Remove any existing std flags that might conflict
  Remove: [-std=*]